<nav class="navbar-lewagon">
  <%= link_to "Add a new list", new_list_path, class: "btn btn-primary button-main" %>

</nav>
<div class="index-background">
  <h1>There are all the lists</h1>


  <% @lists.each do |list| %>
  <ul>
    <li><%= link_to list.name, list_path(list.id) %></li>
  </ul>
  <% end %>
</div>
